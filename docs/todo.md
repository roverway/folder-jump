# 待办事项

本文档记录了 2026-04-04 评审及后续修复后仍需跟进的工作。

## 待处理事项

### 1. Total Commander 面板方位语义

状态：部分解决

- `panelRole` 已建模并暴露。
- `panelSide` 仍通过启发式方法推导。
- 由于当前推断方法不够可靠，左右标签已从 UI 中移除。

下一步：
- 验证 Total Commander 是否提供了比控件位置推断更可靠的左右指示器。

### 2. 文件对话框导航增强

状态：已改进但未完成

- 控件级定位现已存在。
- 快捷键回退现已存在。
- 通用键盘回退仍然存在。

下一步：
- 为现代 Common Item Dialog 控件添加更多特定于宿主程序的验证。
- 考虑采用更强的控件选择策略来发现地址栏。

### 3. Directory Opus 路径收集验证

状态：已改进

- `DOpusRT /info ...,paths` 现为首选方案。
- 标题解析仍作为回退方案。

下一步：
- 在更多 DOpus 版本和布局中验证 XML 格式。
- 决定是保留标题回退还是用第二个官方集成方案替换它。

### 4. 文本编码规范化

状态：部分完成

- 几个关键文件已重新干净地编写。
- 许多较旧的文件和历史文档仍包含乱码。

下一步：
- 将剩余的 `.ahk`、`.md` 和 `.ini` 文件规范化为单一编码标准。
- 推荐目标：如果编辑器/工具需要 BOM，则使用带 BOM 的 UTF-8。

### 5. 评审文档同步

状态：尚未更新

- `docs/code-review-2026-04-04.md` 仍反映评审时的状态。

下一步：
- 在剩余技术事项稳定后，添加一个简短的已解决/未解决附录。
