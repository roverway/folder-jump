# 待办事项

本文档记录当前仍需继续跟进的事项，已与 `docs/code-review-2026-04-04.md` 的最新状态同步。

## 当前优先级

### 1. Total Commander 左右侧语义稳定化

状态：未完全解决

- 当前可以稳定收集 `active / inactive` 两条路径。
- `left / right` 仍缺少稳定、可验证的官方语义来源。
- UI 已移除 `left / right` 展示，避免误导。

后续动作：

- 继续确认 Total Commander 是否存在可靠的窗口消息、控件标识或其他官方信号可用于区分左右侧。
- 在确认可靠方案前，不恢复 `left / right` 标签显示。

### 2. 文件对话框控件识别与结果校验增强

状态：已改进但未完成

- 当前已实现三层跳转策略：控件级优先、快捷键次之、通用按键兜底。
- 现代 Common Item Dialog 的地址栏识别仍有继续增强空间。
- 跳转成功校验目前仍偏启发式。

后续动作：

- 针对常见宿主程序补充更稳的地址栏控件定位规则。
- 在提交路径后增加更可靠的结果校验，减少单纯依赖延时和控件文本猜测。

### 3. Directory Opus 官方接口兼容性验证

状态：已改进

- 当前主方案为 `DOpusRT /info ...,paths`。
- 标题解析仍保留为回退路径。

后续动作：

- 在更多 DOpus 版本、布局和多标签场景下验证返回结构。
- 评估是否保留标题回退，还是改为第二种官方接口兜底。

### 4. 文本文件历史巡检

状态：部分完成

- 仓库已按 UTF-8 with BOM 方向统一过一轮。
- 关键文档与配置已同步清理。
- 仍建议对历史文件做一次更系统的人工巡检。

后续动作：

- 逐个检查旧 `.ahk`、`.md`、`.ini` 文件，确认不存在残留乱码或过期说明。
- 如后续继续统一编码，保持 UTF-8 with BOM 为唯一标准。

## 已完成但无需继续展开的事项

- `Ctrl+G` 触发范围已收紧到文件对话框。
- 文件对话框跳转分层策略已落地。
- 文件对话框识别已改为控件结构优先。
- Windows 路径去重已加入大小写与尾部分隔符归一化。
- `XYplorer` 的“声明支持但未实现”状态已清理。
- 复核报告已补充“已解决 / 未解决”附录。
